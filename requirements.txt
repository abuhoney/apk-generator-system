# BardomPro APK Generator — Python dependencies
flask==3.0.0
gunicorn==21.2.0
# No other third-party deps — the system uses only stdlib for the heavy lifting
openpyxl==3.1.5
pdfplumber==0.11.0
PyPDF2==3.0.1
python-docx==1.1.0
